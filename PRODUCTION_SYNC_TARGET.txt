This directory is a production sync target.

Source of truth:
- local repository / GitHub

Deployment model:
- sync files into /home/admin/clawd/v5-prod
- do not use git pull here as the normal deployment path
- local runtime state lives in .env, .venv, reports, logs, and data

If server-side git metadata is needed again, restore:
- mv .git.disabled .git
