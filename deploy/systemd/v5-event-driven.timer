[Unit]
Description=V5 Event-Driven Trading Check (every 15 minutes)

[Timer]
# Check every 15 minutes, offset from hourly live and auto-risk eval writes.
OnCalendar=*:02/15:00
AccuracySec=30s

# If system was down, run immediately when back up
Persistent=true

[Install]
WantedBy=timers.target
